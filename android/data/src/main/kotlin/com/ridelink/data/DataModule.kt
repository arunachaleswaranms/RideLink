package com.ridelink.data

/**
 * Placeholder establishing the `data` module boundary (ARCHITECTURE §9.1: `database/`,
 * `library/`, `settings/`, `trustedpeers/`). Phase 1a does not implement persistence — see
 * docs/STATUS.md for the exact next task. This object exists only so the module has a
 * compilable source set.
 */
internal object DataModule
