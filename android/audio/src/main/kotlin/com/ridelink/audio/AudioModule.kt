package com.ridelink.audio

/**
 * Placeholder establishing the `audio` module boundary (ARCHITECTURE §9.1: `player/`, `route/`).
 * Phase 1a does not implement audio playback or routing — see docs/STATUS.md for the exact next
 * task. This object exists only so the module has a compilable source set.
 */
internal object AudioModule
